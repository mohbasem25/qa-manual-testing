# Security Test Cases — NexaPay Payment Platform

**Platform under test:** the (fictional) NexaPay payment gateway API and Merchant Portal.

These are **functional security checks a QA engineer runs as part of regular test execution** — verifying that documented, publicly known security expectations (aligned in spirit with **PCI-DSS** data-handling principles and the **OWASP API Security Top 10**) hold up in practice. This suite is **not** a substitute for a full penetration test, formal PCI-DSS assessment, or a dedicated security team's threat-modeling exercise; it covers the checks a manual QA engineer is well positioned to perform during functional test cycles.

**Total test cases:** 28
**Columns:** TC ID | Title | Preconditions | Steps | Test Data | Expected Result | Priority | Type

| TC ID | Title | Preconditions | Steps | Test Data | Expected Result | Priority | Type |
|---|---|---|---|---|---|---|---|
| SEC-001 | Card PAN never appears in application logs | A payment has just been processed with a full card number. | 1. Process a test payment. 2. Search application logs, error logs, and request/response logs generated during the transaction for the raw card number. | Card: 4111111111111111 | No log entry anywhere contains the raw 16-digit PAN; only masked/tokenized references appear. | P1 | Security |
| SEC-002 | Card PAN and CVV never appear in API error responses | A malformed payment request is sent that triggers a validation error. | 1. Submit a payment request with an invalid field alongside a valid card number and CVV. 2. Inspect the full error response body. | Card: 4111111111111111, CVV: 123, invalid currency | Error response describes the validation failure without echoing back the card number or CVV in any field. | P1 | Security |
| SEC-003 | Plaintext HTTP requests to the API are rejected | Environment has both HTTP and HTTPS listeners reachable for testing. | 1. Attempt to call the payments API over `http://` instead of `https://`. | N/A | Connection is refused, or the server issues a redirect to HTTPS with no sensitive data processed over the plaintext channel; TLS is enforced end-to-end. | P1 | Security |
| SEC-004 | Expired/weak TLS versions are rejected | Test client is configured to negotiate TLS 1.0/1.1 or a deprecated cipher suite. | 1. Attempt a handshake using an outdated TLS version. | TLS 1.0 handshake attempt | Handshake fails; only modern TLS versions (1.2+) and strong cipher suites are accepted. | P2 | Security |
| SEC-005 | Merchant cannot view another merchant's transaction via ID manipulation (IDOR) | Merchant A and Merchant B each have their own transactions and valid API credentials. | 1. Authenticate as Merchant A. 2. Request `GET /v1/payments/{id}` using a transaction ID known to belong to Merchant B. | Merchant A's API key + Merchant B's transaction ID | 403 Forbidden or 404 Not Found; no data belonging to Merchant B is returned to Merchant A. | P1 | Security |
| SEC-006 | Merchant cannot list another merchant's transactions by manipulating a merchant_id parameter | Merchant A is authenticated with a valid API key scoped to their own merchant ID. | 1. Call `GET /v1/payments?merchant_id={merchant_B_id}` using Merchant A's credentials. | Merchant A's API key + Merchant B's merchant_id in query string | Server ignores/rejects the mismatched merchant_id and scopes results to the authenticated merchant only, or returns 403; Merchant B's data is never exposed. | P1 | Security |
| SEC-007 | Support-role portal user cannot retrieve full PAN via direct API call | Support-role user has portal access with a limited-scope session/token. | 1. Using the Support user's session token, call the underlying transaction-detail API endpoint directly (bypassing the UI). | Support-role session token | API enforces the same field-level masking as the UI; full PAN is not present in the raw API response for this role either. | P1 | Security |
| SEC-008 | Rate limiting on the login endpoint | Merchant Portal login endpoint is reachable. | 1. Submit repeated login attempts (valid or invalid) in rapid succession from the same source. | 20 requests in 10 seconds | After a defined threshold, further attempts receive `429 Too Many Requests`; limit resets after the configured window. | P2 | Security |
| SEC-009 | Rate limiting on payment creation endpoint | Valid API key with normal usage pattern. | 1. Submit a burst of `POST /v1/payments` requests exceeding the documented per-key rate limit. | Burst above documented limit | Excess requests are throttled with `429` and a `Retry-After` header; legitimate lower-volume traffic is unaffected. | P2 | Security |
| SEC-010 | Brute-force lockout on repeated failed login attempts | Merchant account exists with a known, unlocked state. | 1. Attempt login with incorrect password repeatedly beyond the configured threshold. | 10 consecutive failed attempts | Account or source IP is temporarily locked/throttled; lockout is logged as a security-relevant event; legitimate owner can recover via a defined unlock/reset flow. | P2 | Security |
| SEC-011 | Expired JWT/session token is rejected | A previously issued session token/JWT has passed its expiry time. | 1. Use an expired token to call an authenticated API/portal endpoint. | Expired JWT | 401 Unauthorized; error clearly indicates expiry (not a generic failure that could aid attackers in probing validity) while avoiding overly detailed internals. | P1 | Security |
| SEC-012 | Revoked API key stops working immediately | An API key is revoked via the portal (see UI-KEY-002). | 1. Immediately attempt a request using the just-revoked key. | Revoked API key | 401 Unauthorized without delay/caching window that would allow continued use of a revoked credential. | P1 | Security |
| SEC-013 | SQL-injection-style input on merchant-facing search/filter fields is neutralized | Merchant Portal transaction search field is available. | 1. Enter a SQL-injection-style payload into the transaction search box. 2. Submit the search. | Search input: `' OR '1'='1' --` | Input is treated as a literal search term (parameterized query); no unrelated data is exposed; no HTTP 500 error; no evidence of query manipulation. | P1 | Security |
| SEC-014 | XSS-style payload in a merchant-facing input is neutralized on render | Merchant Portal has a free-text field (e.g., a payment description or metadata note). | 1. Enter a script-tag payload into the field. 2. Save. 3. View the field wherever it is subsequently displayed (list, detail, export). | Input: `<script>alert('xss')</script>` | Payload is stored and rendered as inert, HTML-escaped text; no script execution occurs anywhere it is displayed. | P1 | Security |
| SEC-015 | Webhook payloads without a valid signature are rejected by the merchant-side verification contract | A webhook event is crafted without the expected signature header, simulating an attacker probing the merchant's endpoint. | 1. Send a fabricated `payment.captured` event to a test webhook receiver without a valid signature. 2. Send the same event with a tampered payload but a stale/incorrect signature. | Unsigned payload; tampered payload + invalid signature | A correctly implemented webhook consumer rejects both; NexaPay's own documentation/reference implementation demonstrates HMAC signature verification (e.g., comparing a computed HMAC-SHA256 digest against the provided signature header) before trusting any webhook payload. | P1 | Security |
| SEC-016 | Sensitive fields are masked consistently between UI and API responses | A transaction with card data exists. | 1. View the transaction in the Merchant Portal UI. 2. Call the equivalent API endpoint with the same credentials. 3. Compare masking of PAN/CVV fields. | Same transaction viewed via UI and API | Masking is identical and consistent across both surfaces (e.g., both show `**** 1111`, never full PAN); no channel is more permissive than the other. | P2 | Security |
| SEC-017 | Session fixation is prevented on login | An unauthenticated session/cookie exists before login. | 1. Capture the session identifier before authentication. 2. Log in successfully. 3. Compare the session identifier after login. | Pre-login vs. post-login session ID | A new session identifier is issued upon successful authentication; the pre-login session ID is invalidated and cannot be reused to hijack the authenticated session. | P2 | Security |
| SEC-018 | Idle session timeout logs the user out of the Merchant Portal | Merchant Portal idle-timeout is configured to a short interval for test purposes. | 1. Log in. 2. Leave the portal idle beyond the timeout window. 3. Attempt an authenticated action. | Idle timeout: 2 minutes (test config) | Session is invalidated after the idle window; the next action requires re-authentication rather than silently succeeding on a stale session. | P2 | Security |
| SEC-019 | Insufficient-scope API key cannot perform a privileged action even via a differently named endpoint alias | API key scoped only to `payments:read`. | 1. Attempt the primary restricted endpoint (already covered in API-ERR-004). 2. Attempt any known alias/legacy path that performs the same underlying action. | payments:read-only key | All paths performing the privileged action enforce the same scope check consistently; no alias/legacy route bypasses authorization. | P2 | Security |

## NexaPay Wallet — Security

These functional security checks extend the same black-box QA approach to **NexaPay Wallet**, the consumer digital-wallet product, focused on P2P transfer misdirection risk, wallet balance tamper/replay resistance, and bill-payment input handling.

| TC ID | Title | Preconditions | Steps | Test Data | Expected Result | Priority | Type |
|---|---|---|---|---|---|---|---|
| SEC-P2P-001 | P2P recipient validation prevents IDOR-style misdirected transfers | Consumer A is authenticated in the Wallet app/API; wallet C belongs to an unrelated third user. | 1. Submit a P2P transfer request with a manipulated `recipient_wallet_id` pointing to wallet C instead of the looked-up/confirmed recipient. | Sender: consumer A; tampered recipient_wallet_id=wallet_C | Server re-validates that the recipient matches a confirmed lookup for the request (not just a client-supplied ID); a mismatched/unconfirmed recipient ID is rejected with a clear error, and no funds move to wallet C. | P1 | Security |
| SEC-P2P-002 | P2P transfer amount/recipient cannot be tampered with between confirmation and submission | Consumer has reached the transfer confirmation screen for recipient B, amount 50. | 1. Intercept and modify the submission request to change the amount or recipient after confirmation was displayed for different values. | Confirmed: recipient B, amount 50; submitted: recipient D, amount 500 | Server-side validation detects the mismatch against the confirmed transfer intent (e.g., a signed/short-lived confirmation token) and rejects the tampered request; no transfer is executed with the altered values. | P1 | Security |
| SEC-P2P-004 | Wallet-to-wallet transfer requires step-up re-authentication for high-value transfers | A high-value transfer threshold (e.g., AED 5,000) is configured to require step-up authentication (PIN/biometric/OTP). | 1. Attempt a P2P transfer above the configured threshold without completing step-up authentication. | Amount: AED 7,500 (above threshold) | Transfer is blocked pending successful step-up authentication; bypassing the step-up prompt via a direct API call is also rejected, not just hidden in the UI. | P2 | Security |
| SEC-WAL-001 | Wallet balance value cannot be spoofed via client-side request tampering | Consumer's wallet balance is authoritative server-side. | 1. Attempt to submit a spend/transfer request that includes a client-supplied `balance` or `available_funds` field asserting a higher balance than actually exists. | Client-asserted balance: 999999 (does not match server record) | Server ignores any client-supplied balance field entirely and validates spend against its own authoritative balance record; the spoofed value has no effect on the outcome. | P1 | Security |
| SEC-WAL-002 | Replayed top-up confirmation webhook does not double-credit the wallet | A funding-source (card/bank) confirmation webhook for a completed top-up is captured. | 1. Redeliver the identical top-up confirmation webhook payload and signature a second time. | Same event_id and signature, redelivered | Wallet is credited only once for the original event; the replayed delivery is recognized as a duplicate (via event_id/idempotency check) and results in a no-op, not a second credit. | P1 | Security |
| SEC-WAL-003 | Wallet PIN/passcode brute-force lockout on repeated failed attempts | Wallet app requires a PIN/passcode to authorize sensitive actions (e.g., transfers, bill payments). | 1. Submit an incorrect wallet PIN repeatedly beyond the configured threshold. | 5 consecutive incorrect PIN attempts | Wallet PIN entry is temporarily locked/throttled after the threshold; lockout is logged as a security-relevant event; legitimate owner can recover via a defined reset flow (e.g., full re-authentication). | P2 | Security |
| SEC-P2P-003 | Rate limiting on repeated P2P transfer attempts | Wallet account is authenticated and has a documented per-account transfer rate limit. | 1. Submit a burst of P2P transfer requests from the same wallet account well above its configured rate limit. | Burst of transfer requests exceeding the documented limit | Excess requests receive `429 Too Many Requests` with a `Retry-After` header; legitimate lower-volume traffic from the same or other wallets is unaffected. | P2 | Security |
| SEC-BILL-001 | Biller reference field is neutralized against injection-style payloads | Wallet app's bill-payment reference-entry field is available. | 1. Enter a SQL-injection- or command-injection-style payload into the biller reference field. 2. Submit the payment attempt. | Reference input: `' OR '1'='1' --` | Input is treated as a literal, invalid reference value (parameterized handling); request is rejected with a standard `INVALID_BILLER_REFERENCE` validation error; no HTTP 500 error or evidence of query/command manipulation. | P1 | Security |
| SEC-BILL-002 | Bill payment provider callback signature is verified before crediting/marking a payment successful | A biller/provider confirmation callback is crafted without a valid signature, simulating an attacker probing the callback endpoint. | 1. Send a fabricated `bill_payment.confirmed` callback without a valid signature. 2. Send the same callback with a tampered payload and a stale/incorrect signature. | Unsigned payload; tampered payload + invalid signature | Both are rejected before the associated bill payment is marked successful or the wallet is debited; NexaPay's reference implementation verifies an HMAC signature (or equivalent) before trusting any provider callback. | P1 | Security |

## Notes

This suite intentionally stays at the level of functional, black-box security checks appropriate for a manual QA engineer's regular test cycles. It references PCI-DSS and the OWASP API Security Top 10 only as general, publicly available frameworks for categorizing risk areas — it makes no claim that NexaPay (a fictional platform) holds any compliance certification, and it is not a replacement for formal penetration testing or a compliance audit.
